{
  "name": "scheduling-api",
  "version": "1.0.0",
  "description": "RESTful API for multi-tenant scheduling management",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "check": "node --check index.js && node --check src/app.js && node --check src/routes/index.js && node --check src/middlewares/validators.js && node --check test/app.test.js",
    "test": "node --test",
    "format": "prettier --write ."
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "http-status-codes": "^2.3.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "prettier": "^3.8.3"
  }
}
